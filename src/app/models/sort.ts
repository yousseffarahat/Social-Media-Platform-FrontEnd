export class Sort {
  sorted: string;
  unsorted: string;
  empty: string;

  constructor(sorted: string, unsorted: string, empty: string) {
    this.sorted = sorted;
    this.unsorted = unsorted;
    this.empty = empty;
  }
}
